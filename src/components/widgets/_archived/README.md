# Archived floating widgets

These widgets are **not mounted** in the app. Code is kept for possible revival.

| Widget | Status | Replacement |
|--------|--------|-------------|
| `SystemInfoWidget` | Removed from UI | — |

Do not re-export from `../index.js` until product wants them back.
